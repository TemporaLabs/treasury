{
  "name": "@temporalabs/treasury",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TemporaLabs/treasury.git"
  },
  "files": [
    "LICENSE",
    "NOTICE",
    "THIRD_PARTY_NOTICES.md",
    "dist",
    "registry"
  ],
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "description": "Agent Treasury by Tempora Labs — a depositor client for ERC-4626 vaults: registry, pre-flight, and unsigned deposit/withdraw builders. Never signs, never holds a key.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "treasury-mcp": "./dist/mcp-server.mjs"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prepack": "npm run build && git diff --exit-code -- dist/mcp-server.mjs",
    "build": "tsc -b && tsup && tsx scripts/third-party-notices.ts --write",
    "typecheck": "tsc -b && tsc -p tsconfig.scripts.json",
    "test": "tsc -b && tsc -p tsconfig.scripts.json && vitest run",
    "test:fork": "tsc -b && TREASURY_FORK=1 vitest run --testTimeout 300000 tests/fork.test.ts",
    "mcp": "tsx src/mcp/server.ts",
    "registry:check": "tsx scripts/registry-check.ts",
    "notices:check": "tsx scripts/third-party-notices.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0",
    "viem": "^2.37.0",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsup": "^8.5.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^5.0.0"
  }
}
